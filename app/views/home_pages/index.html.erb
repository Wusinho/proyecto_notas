
<div class="row row-cols-1 row-cols-md-4 g-4">
  <%= render 'notes/search_bar', locals: { path: home_pages_path } %>
  <%= render 'notes/order_notes' %>
  <% @notes.each do |note| %>
    <%= render 'card', note: note %>
  <% end %>
</div>

